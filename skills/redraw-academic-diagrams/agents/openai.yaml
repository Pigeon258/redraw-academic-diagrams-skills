interface:
  display_name: "可编辑科研图重绘主版本"
  short_description: "严格科研图重绘主版本，自动选择默认或30分钟内快速预设"
  default_prompt: "Use $redraw-academic-diagrams 按我明确指定的 DEFAULT 或 FAST 预设重绘科研图；若未指定，则在硬时间预算不超过 30 分钟时采用 FAST，否则采用 DEFAULT。"

policy:
  allow_implicit_invocation: true
