interface:
  display_name: "科研图极速重绘（已暂停）"
  short_description: "该极速试验版已暂停使用，新任务与后续迭代请改用当前主版本"
  default_prompt: "Do not use $redraw-academic-diagrams-fast for new work; use $redraw-academic-diagrams in fast delivery mode instead."

policy:
  allow_implicit_invocation: false
